package game;
/**
 * Enum representing the type of a player in the game.
 * Players can either be controlled by a human (REAL) or by the system (ARTIFICIAL).
 */

public enum PlayerType {
    ARTIFICIAL,
    REAL;


}
